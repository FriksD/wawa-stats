# WAWA 小说数据记录与统计

一个为 `https://wawawriter.com/app/submission` 投稿页设计的本地数据记录工具。

这个网站本身没有历史数据统计功能，因此本脚本会在你打开投稿页时自动记录每日的小说数据，并提供一个直观的统计面板，帮助你观察字数增长、收益变化和在读人数趋势。

---

## 功能特性

- **自动记录每日数据**
  - 自动获取每本书的章节数、总字数、状态、最近更新。
  - 自动获取历史总收益、单日收益、昨日变化、在读人数。

- **适配网站“延迟一天”的数据更新机制**
  - 网站 14:30 后刷新的数据，实际上是前一天的收益。
  - 例如：8 月 18 日 14:30 后拿到的是 8 月 17 日的数据。
  - 脚本会按接口返回的真实统计日期保存，不会把日期搞混。

- **本地持久化保存**
  - 数据只保存在你的浏览器本地，不会上传到任何服务器。
  - 脚本更新不会丢失历史数据。

- **全局概览视图**
  - 默认打开就是所有书的整体数据。
  - 今日哪些书产生了收益、今日总收益、所有书总收益、今日总在读人数，一眼可见。
  - 全站单日收益趋势折线图。
  - 今日收益占比饼图。
  - 在读人数分布饼图。
  - 全部书籍总览表格。

- **单书详情视图**
  - 支持查看每本书的历史趋势。
  - 可切换：历史总收益、单日收益、在读人数、今日新增在读、总字数、章节数。

- **CSV 导出**
  - 全局导出：所有书、所有日期的完整数据。
  - 单书导出：当前选中书的完整历史数据。

- **手动采集**
  - 页面右下角提供手动采集按钮。
  - 需要补采时也可以手动强制记录当前快照。

---

## 安装方法

1. 安装浏览器扩展 Tampermonkey（Edge/Chrome/Firefox 均可）。
2. 打开本脚本的 Greasy Fork 页面：`https://greasyfork.org/zh-CN/scripts/591889`，点击“安装此脚本”。
3. 安装完成后，打开并登录：
   `https://wawawriter.com/app/submission`
4. 刷新页面，脚本会自动开始工作。

---

## 使用方法

### 自动记录

- 打开投稿页后，脚本会自动采集一次当天可获取的最新数据。
- 由于网站数据延迟一天，脚本会按照接口返回的真实数据日期保存，不会反复请求。

### 手动采集

页面右下角有两个圆形按钮：

- `📥`：立即采集当前数据。
- `📊`：打开数据统计面板。

如果在 14:30 前手动采集，会弹出确认框，因为此时网站数据可能还是前一天的。

### 统计面板

打开统计面板后，默认显示“全局概览”：

- 顶部汇总卡片：数据日期、记录天数、追踪书籍数、今日新增收益、今日总收益、所有书总收益、今日总在读、今日新增总在读。
- 全站单日收益趋势：查看所有书每天合计收益的变化。
- 今日收益占比：饼图展示今天有收益的书分别占多少。
- 在读人数分布：饼图展示各书在读人数占比。
- 今日有收益的书：直接列出今天产生收益的书。
- 全部书籍总览：所有书的最新状态、字数、收益、在读，并可点击“查看”进入单书详情。

在“单书详情”标签页中：

- 选择一本书。
- 选择指标：历史总收益 / 单日收益 / 在读人数 / 今日新增在读 / 总字数 / 章节数。
- 查看折线图和每日明细表。

### 导出 CSV

- 在“全局概览”点击“导出 CSV”：导出所有书所有日期的数据。
- 在“单书详情”点击“导出 CSV”：只导出当前这本书的数据。

---

## 数据存储说明

- 数据通过 Tampermonkey 的 `GM_setValue` 保存在浏览器本地。
- 刷新页面、关闭浏览器、更新脚本，都不会丢失数据。
- 数据与脚本的 `@name` 和 `@namespace` 绑定，请勿随意修改这两个标识。
- 如果卸载脚本或清理浏览器数据，本地历史数据可能会丢失，建议定期导出 CSV 备份。

---

## 隐私说明

- 脚本只请求 `wawawriter.com` 同源接口，用于获取你自己的投稿数据。
- 所有记录数据仅保存在本地浏览器中。
- 脚本不会向任何第三方服务器上传数据，也没有任何统计追踪代码。

---

## 常见问题

### 1. 为什么打开页面后没有自动记录？

可能原因：

- 当前数据已经记录过了，脚本不会重复采集。
- 当前时间早于 14:30，且还没有可补采的历史数据。
- 页面卡片还没有加载完成，稍等几秒。

### 2. 数据日期为什么显示的是昨天？

因为网站的数据本身就是延迟一天的：

- 8 月 18 日 14:30 后刷新出来的是 8 月 17 日的数据。
- 8 月 18 日的数据要等 8 月 19 日 14:30 后才出来。

这是正常现象，脚本会按真实数据日期保存。

### 3. 能补采安装之前的历史数据吗？

不能。脚本只能从安装后开始记录，无法获取过去的历史数据。

### 4. 更新脚本会丢数据吗？

不会。只要保持脚本的 `@name` 和 `@namespace` 不变，更新版本不会影响本地数据。

### 5. 如何备份数据？

打开统计面板，点击“导出 CSV”即可下载备份文件。

---

## 更新日志

### 0.3.0

- 全局概览卡片改为 8 个，并优化为对称动态布局。
- 新增“今日新增收益”“今日新增总在读”指标。
- 单书详情新增“今日新增在读”折线图。
- 折线图缺失数据按 0 处理，Y 轴上限改为动态向上取整。

### 0.2.2

- 修复网站“延迟一天”的数据更新逻辑。
- 已采集到当前最新日期数据后，不再反复请求。

### 0.2.1

- 修复自动采集导致的轮询问题。
- 不需要采集时直接断开页面监听。

### 0.2.0

- 全新统计界面。
- 新增全局概览、收益占比饼图、在读人数分布图、今日有收益书列表。
- 优化单书详情入口。

### 0.1.0

- 初始版本。
- 实现每日数据自动记录、本地存储、基础统计面板。
