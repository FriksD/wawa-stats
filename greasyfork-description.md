# WAWA 小说数据记录与统计

一个为 `https://wawawriter.com/app/submission` 投稿页设计的本地数据记录工具。

这个网站本身没有历史数据统计功能，因此本脚本会在你打开投稿页时自动记录每日的小说数据，并提供一个直观的统计面板，帮助你观察字数增长、收益变化和在读人数趋势。

---

## 功能特性

- **自动记录每日数据**
  - 自动获取每本书的章节数、总字数、状态、最近更新。
  - 自动获取历史总收益、单日收益、昨日变化、在读人数。

- **适配网站“延迟一天”的数据更新机制**
  - 网站每天 14:00 起陆续刷新数据（每本书更新时间不同，可能持续到 14:30 之后），刷新出来的是前一天的收益。
  - 例如：8 月 18 日下午拿到的是 8 月 17 日的数据。
  - 每本书都有自己的 `latest.date` 时间戳，脚本会按每本书自己的时间戳一一对应保存，绝不把新旧日期的数据混进同一天。

- **智能静默同步**
  - 打开页面自动静默采集一次，无任何弹窗打扰。
  - 若有书还没更新到最新数据日期，脚本会在 14:00~17:00 窗口内每 5 分钟静默重采，直到所有书都拿到最新数据。
  - 全部到位后自动停止，等待下一个 14:00 更新窗口；标签页挂在后台时不发请求。

- **本地持久化保存**
  - 数据只保存在你的浏览器本地，不会上传到任何服务器。
  - 脚本更新不会丢失历史数据。

- **全局概览视图**
  - 默认打开就是所有书的整体数据。
  - 今日哪些书产生了收益、今日总收益、所有书总收益、总在读人数，一眼可见。
  - “更新进度”卡片：更新窗口期显示 X/Y 本已更新，“今日”类统计只计入已更新的书，不混旧数据。
  - 全站单日收益趋势折线图。
  - 全站在读人数趋势折线图。
  - 全站累计总收益趋势折线图。
  - 近 7 日 vs 前 7 日环比：收益与新增在读的涨跌对比。
  - 月度统计：按月汇总总收益与新增在读，例如直接查看 7 月份的总收益。
  - 今日收益占比饼图。
  - 在读人数分布饼图。
  - 全部书籍总览表格：显示每本书的数据日期、最新状态、字数、收益、在读人数、新增在读，未更新的书带灰色标记，并支持删除某本书的全部历史数据。

- **单书详情视图**
  - 单日收益趋势和在读人数趋势两张折线图同时显示，无需切换。
  - 顶部摘要卡：最新数据日期、累计总收益、最新单日收益、在读人数、最新新增在读、已记录天数。
  - 每日明细表：日期 / 单日收益 / 累计总收益 / 在读 / 新增在读，最新日期在最上面。

- **CSV 导出**
  - 全局导出：所有书、所有日期的完整数据。
  - 单书导出：当前选中书的完整历史数据。

- **手动采集**
  - 页面右下角提供手动采集按钮。
  - 需要补采时也可以手动强制记录当前快照。

---

## 安装方法

1. 安装浏览器扩展 Tampermonkey（Edge/Chrome/Firefox 均可）。
2. 打开本脚本的 Greasy Fork 页面：`https://greasyfork.org/zh-CN/scripts/591889`，点击“安装此脚本”。
3. 安装完成后，打开并登录：
   `https://wawawriter.com/app/submission`
4. 刷新页面，脚本会自动开始工作。

---

## 使用方法

### 自动记录

- 打开投稿页后，脚本会自动静默采集一次当天可获取的最新数据。
- 网站数据每天 14:00 起陆续更新，若有书还没更新，脚本会在 14:00~17:00 窗口内每 5 分钟静默重采，直到所有书都拿到最新数据，全程无感知。
- 脚本按接口返回的真实数据日期保存，同一天的数据不会重复记录。

### 手动采集

页面右下角有两个圆形按钮：

- `📥`：立即采集当前数据。
- `📊`：打开数据统计面板。

手动采集在任何时间点击都是安全的——脚本按每本书自己的数据日期入库，不会记错日期。

### 统计面板

打开统计面板后，默认显示“全局概览”：

- 顶部汇总卡片：数据日期、更新进度、追踪书籍数、记录天数、今日总收益、今日有收益书数、所有书总收益、总在读、今日新增在读。
- 更新窗口期（14:00~14:30+）有的书先更新、有的书后更新，“今日”类统计只计入已更新到最新日期的书，“更新进度”卡片会显示 X/Y 本；全部更新后显示“已全部更新”。
- 今日新增在读 = 当天在读人数 − 前一天在读人数；如果前一天没有数据，则显示“无”，不会把前一天默认成 0 来计算。
- 全站单日收益趋势：所有书每天合计收益的变化。
- 全站在读人数趋势：所有书在读人数合计的变化。
- 全站累计总收益趋势：历史总收益的累计增长曲线。
- 近 7 日 vs 前 7 日：收益与新增在读的环比涨跌。
- 月度统计：按月查看总收益、新增在读和记录天数。
- 今日收益占比 / 在读人数分布：饼图展示各书占比。
- 今日有收益的书：直接列出今天产生收益的书。
- 全部书籍总览：所有书的最新状态、字数、收益、在读、新增在读，未更新的书带“未更新”灰色标记，可点击“查看”进入单书详情。

在“单书详情”标签页中：

- 选择一本书。
- 单日收益趋势和在读人数趋势两张折线图同时显示。
- 顶部摘要卡 + 每日明细表（最新日期在最上面）。

### 导出 CSV

- 在“全局概览”点击“导出 CSV”：导出所有书所有日期的数据。
- 在“单书详情”点击“导出 CSV”：只导出当前这本书的数据。

---

## 数据存储说明

- 数据通过 Tampermonkey 的 `GM_setValue` 保存在浏览器本地。
- 刷新页面、关闭浏览器、更新脚本，都不会丢失数据。
- 数据与脚本的 `@name` 和 `@namespace` 绑定，请勿随意修改这两个标识。
- 如果卸载脚本或清理浏览器数据，本地历史数据可能会丢失，建议定期导出 CSV 备份。

---

## 隐私说明

- 脚本只请求 `wawawriter.com` 同源接口，用于获取你自己的投稿数据。
- 所有记录数据仅保存在本地浏览器中。
- 脚本不会向任何第三方服务器上传数据，也没有任何统计追踪代码。

---

## 常见问题

### 1. 为什么打开页面后没有自动记录？

可能原因：

- 当前数据已经记录过了，脚本不会重复采集。
- 页面卡片还没有加载完成，稍等几秒。

### 2. 数据日期为什么显示的是昨天？

因为网站的数据本身就是延迟一天的：

- 8 月 18 日下午刷新出来的是 8 月 17 日的数据。
- 8 月 18 日的数据要等 8 月 19 日下午才出来。

这是正常现象，脚本会按真实数据日期保存。

### 3. 能补采安装之前的历史数据吗？

不能。脚本只能从安装后开始记录，无法获取过去的历史数据。

### 4. 更新脚本会丢数据吗？

不会。只要保持脚本的 `@name` 和 `@namespace` 不变，更新版本不会影响本地数据。

### 5. 如何备份数据？

打开统计面板，点击“导出 CSV”即可下载备份文件。

---

## 更新日志

### 0.6.0

- 修复更新窗口期新旧日期数据混合的问题：按每本书自己的 `latest.date` 分组入库，历史混日期数据自动迁移拆分。
- “今日”类统计（今日总收益 / 今日有收益 / 今日新增在读 / 收益占比）只计入已更新到最新日期的书，新增“更新进度 X/Y 本”卡片，未更新的书在总览表带灰色标记。
- 自动同步改为智能静默轮询：14:00~17:00 窗口内每 5 分钟静默重采，直到所有书拿到最新数据，全程无感知；后台标签页不发请求。
- 移除“各书总收益排行”柱状图，新增“全站累计总收益趋势”折线图、“近 7 日 vs 前 7 日”环比、月度统计（按月查看总收益）。
- 单书详情重构：单日收益 + 在读人数两张折线图固定同时显示，新增摘要卡，明细表精简为 5 列并按最新日期倒序；CSV 导出增加“新增在读”列。
- API 解析改为按接口真实字段精确取值，新增在读直接由接口 `previous` 数据算出。
- 手动采集移除确认弹窗（按日期入库后任何时间点采集都安全）；修复统计弹窗开着时采集后不刷新的问题。

### 0.5.0

- 全部书籍总览支持删除某本书的全部历史数据。
- 新增“全站在读趋势”折线图。
- 新增“各书总收益排行”柱状图。

### 0.4.3

- 单书详情复用全局概览的真实时间戳逻辑，只显示有 `statDate` 的数据，不再混入本地日期/虚假日期。

### 0.4.2

- 全站收益趋势进一步过滤历史遗留的零星日期记录，避免出现 8/13、8/19 这类虚假点。

### 0.4.1

- 修复全站收益趋势图出现零散日期（如 8/13、8/19）的问题。
- 每次采集统一归到当前最新数据日期，同时每本书保留自己的时间戳。
- 折线图改用自定义悬浮提示，不再错误固定显示第一个点。

### 0.4.0

- 每本书按自己的 `latest.date` 时间戳分别保存，不再按“多数日期”统一记录。
- 全部书籍总览新增“数据日期”和“新增在读”列。
- “今日新增总在读”改为使用接口返回的 `previousReaders` 计算，每本书对应自己的前一条数据。
- 自动采集不再判断“今天该不该采”，每个页面会话采一次，按时间戳归档。

### 0.3.2

- 修复“今日新增总在读”：前一天没有数据时显示“无”/0，不再错误地把前一天默认成 0 来计算。

### 0.3.1

- 修正卡片：恢复“今日有收益书数”，移除错误的“今日新增收益”卡片。
- 保留“今日新增总在读”指标。

### 0.3.0

- 全局概览卡片改为 8 个，并优化为对称动态布局。
- 新增“今日新增总在读”指标。
- 单书详情新增“今日新增在读”折线图。
- 折线图缺失数据按 0 处理，Y 轴上限改为动态向上取整。

### 0.2.2

- 修复网站“延迟一天”的数据更新逻辑。
- 已采集到当前最新日期数据后，不再反复请求。

### 0.2.1

- 修复自动采集导致的轮询问题。
- 不需要采集时直接断开页面监听。

### 0.2.0

- 全新统计界面。
- 新增全局概览、收益占比饼图、在读人数分布图、今日有收益书列表。
- 优化单书详情入口。

### 0.1.0

- 初始版本。
- 实现每日数据自动记录、本地存储、基础统计面板。
